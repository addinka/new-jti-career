import { ApplicationStatusDirective } from './application-status.directive';

describe('ApplicationStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicationStatusDirective();
    expect(directive).toBeTruthy();
  });
});
