export class HotRoles {
    public id: string;
    public name: string;
    public index: number;

    constructor(id: string, name: string, index: number) {
        this.id = id;
        this.name = name;
        this.index = index;
    }
}
